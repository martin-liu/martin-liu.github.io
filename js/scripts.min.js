(function() {
  window.Config = {
    name: "blog",
    debug: false,
    uri: {
      base: "/",
      host: "/",
      api: "https://api.github.com/"
    },
    githubRepo: {
      name: "martin-liu.github.io",
      owner: "martin-liu"
    },
    "default": {
      displayCount: 10,
      modalAnimateIn: "fadeInRight",
      modalAnimateOut: "fadeOutRight"
    },
    piwik: {
      enabled: false,
      app: 'appName',
      prod: 'prodName',
      url: '',
      siteId: 0
    },
    intro: {
      enabled: false
    },
    urlHtml5Mode: false,
    version: "0.0.1"
  };

}).call(this);

(function() {
  Config.intro = [
    {
      intro: "Hello World!",
      position: 'bottom'
    }, {
      intro: "This is the header",
      position: 'bottom'
    }
  ];

}).call(this);

(function() {
  Config.routes = [
    {
      url: "/",
      params: {
        name: "home",
        label: "Home",
        templateUrl: "partials/home.html",
        controller: "HomeCtrl"
      }
    }, {
      url: "/article/:id",
      params: {
        name: "article",
        hide: true,
        templateUrl: "partials/article.html",
        controller: "ArticleCtrl"
      }
    }, {
      url: "/about",
      params: {
        name: "about",
        label: "About",
        templateUrl: "partials/about.html"
      }
    }
  ];

}).call(this);

(function() {
  angular.module("config", []).config(["$routeProvider", "$locationProvider", "$logProvider", function($routeProvider, $locationProvider, $logProvider) {
    var isHtml5Mode;
    if (angular.isUndefined(Config.debug) || Config.debug !== false) {
      Config.debug = true;
    }
    if (angular.isUndefined(Config.development) || Config.development !== false) {
      Config.development = true;
    }
    if (Config.debug === false) {
      $logProvider.debugEnabled(false);
    }
    angular.forEach(Config.routes, function(route) {
      if (route.params && !route.params.controller) {
        route.params.controller = 'BaseCtrl';
      }
      $routeProvider.when(route.url, route.params);
    });
    $routeProvider.otherwise({
      templateUrl: 'partials/404.html'
    });
    isHtml5Mode = !!Config.urlHtml5Mode && Modernizr.history;
    $locationProvider.html5Mode(isHtml5Mode);
    $locationProvider.hashPrefix('!');
  }]);

  window.onload = function() {
    var registerController;
    if (angular.isUndefined(Config.name)) {
      console.error("Config.name is undefined, please update config.json to include this property.");
    }
    registerController = (function() {
      var ctrlSet;
      ctrlSet = {};
      return function(ctrl) {
        var name;
        if (!ctrlSet[ctrl]) {
          name = ctrl.substring(0, ctrl.indexOf('Ctrl'));
          angular.module(Config.name).controller(ctrl, [
            '$scope', "" + name + "ViewModel", function(scope, ViewModel) {
              return scope.vm = new ViewModel(scope);
            }
          ]);
          return ctrlSet[ctrl] = 1;
        }
      };
    })();
    angular.forEach(Config.routes, function(route) {
      var ctrl;
      ctrl = route.params.controller;
      if (ctrl == null) {
        ctrl = 'BaseCtrl';
      }
      return registerController(ctrl);
    });
    return angular.bootstrap(document, [Config.name]);
  };

}).call(this);

(function() {
  
  angular.module('m-util', []).factory('Util', ["$modal", "$timeout", "$location", "$anchorScroll", "$window", "$http", "$templateCache", "$compile", "$q", "$route", "Cache", function($modal, $timeout, $location, $anchorScroll, $window, $http, $templateCache, $compile, $q, $route, Cache) {
    var Util;
    Util = (function() {
      var alertTpl;

      function Util() {}

      alertTpl = 'partials/modal/alert.html';

      Util.prototype.createDialog = function(template, scope, thenFunc, options) {
        var dialog;
        if (options == null) {
          options = {};
        }
        options = angular.extend({
          backdropFade: true,
          templateUrl: template,
          controller: [
            "$scope", "$modalInstance", "scope", function($scope, $modalInstance, scope) {
              $scope = angular.extend($scope, scope);
              $scope.animate = Config["default"].modalAnimateIn;
              return $scope.close = function(data) {
                $scope.animate = Config["default"].modalAnimateOut;
                return $timeout((function() {
                  return $modalInstance.close(data);
                }), 500);
              };
            }
          ],
          resolve: {
            scope: function() {
              return scope;
            }
          }
        }, options);
        dialog = $modal.open(options);
        dialog.result.then(thenFunc);
        return dialog;
      };

      Util.prototype.alert = function(message, type, thenFunc) {
        var mclass, scope;
        mclass = (function() {
          switch (type) {
            case 'success':
              return 'alert-success';
            case 'fail':
              return 'alert-danger';
            case 'confirm':
              return 'alert-warning';
          }
        })();
        scope = {
          message: message,
          type: type,
          "class": mclass
        };
        return this.createDialog(alertTpl, scope, thenFunc);
      };

      Util.prototype.success = function(message, thenFunc) {
        return this.alert(message, 'success', thenFunc);
      };

      Util.prototype.fail = function(message, thenFunc) {
        return this.alert(message, 'fail', thenFunc);
      };

      Util.prototype.confirm = function(message, thenFunc) {
        return this.alert(message, 'confirm', thenFunc);
      };

      Util.prototype.compileTemplate = function(templateUrl, scope) {
        var defer, loader;
        defer = $q.defer();
        loader = $http.get(templateUrl, {
          cache: $templateCache
        });
        loader.success(function(html) {
          return defer.resolve($compile(html)(scope));
        });
        return defer.promise;
      };

      Util.prototype.toggleFullscreen = function(e) {
        angular.element(document.body).toggleClass('fullscreenStatic');
        return angular.element(e).toggleClass('fullscreen');
      };

      Util.prototype.daysBetween = function(date1, date2) {
        var date1_ms, date2_ms, difference_ms, one_day;
        one_day = 1000 * 60 * 60 * 24;
        date1_ms = date1.getTime();
        date2_ms = date2.getTime();
        difference_ms = Math.abs(date2_ms - date1_ms);
        return Math.round(difference_ms / one_day);
      };

      Util.prototype.formatDate = function(date) {
        if (date === '0000-00-00 00:00:00') {
          return '';
        } else {
          return date;
        }
      };

      Util.prototype.truncDate = function(date) {
        if (!date || date === '0000-00-00') {
          return '';
        } else {
          return date.match(/\S+/g)[0];
        }
      };

      Util.prototype.truncateCharacter = function(input, chars, breakOnWord) {
        var lastspace;
        if (isNaN(chars)) {
          return input;
        }
        if (chars <= 0) {
          return '';
        }
        if (input && input.length >= chars) {
          input = input.substring(0, chars);
          if (!breakOnWord) {
            lastspace = input.lastIndexOf(' ');
            if (lastspace !== -1) {
              input = input.substr(0, lastspace);
            }
          } else {
            while (input.charAt(input.length - 1) === ' ') {
              input = input.substr(0, input.length(-1));
            }
          }
          return input + '...';
        }
        return input;
      };

      Util.prototype.redirect = function(path, force) {
        path = path.replace('%', '');
        if ($location.url() !== path) {
          return $location.url(path);
        } else {
          if (force) {
            return $route.reload();
          }
        }
      };

      Util.prototype.reload = function() {
        return $route.reload();
      };

      Util.prototype.scrollTo = function(height) {
        return $window.scrollTo(0, height);
      };

      Util.prototype.setUpScrollHandler = function(elId, event) {
        return $timeout(function() {
          var el;
          el = document.getElementById(elId);
          if (el) {
            $anchorScroll();
            return angular.element(el).triggerHandler(event);
          }
        }, 0);
      };

      Util.prototype.getParams = function() {
        return $location.search();
      };

      Util.prototype.getUserParam = function(user) {
        return {
          nt: user.nt,
          fullname: user.displayName,
          label: user.label
        };
      };

      Util.prototype.getWithCache = function(key, isSession, getFunc, timeout) {
        var cache, data, defer, promise;
        cache = Cache;
        if (isSession) {
          cache = Cache.session;
        }
        defer = $q.defer();
        data = cache.get(key);
        if (data) {
          defer.resolve(data);
          return defer.promise;
        } else {
          promise = getFunc();
          promise.then(function(data) {
            return cache.set(key, data, timeout);
          });
          return promise;
        }
      };

      Util.prototype.capitalize = function(str) {
        return str.charAt(0).toUpperCase() + str.slice(1);
      };

      Util.prototype.solrEscape = function(q) {
        return q.replace(/[+\-\!\(\)\{\}\[\]\^"~\*\?:\\]+/g, ' ');
      };

      Util.prototype.removeHtmlTags = function(str) {
        return str.replace(/<[^>]*>?/g, '');
      };

      Util.prototype.waitUntil = function(func, check, interval, maxTime) {
        var doWait;
        if (interval == null) {
          interval = 300;
        }
        if (maxTime == null) {
          maxTime = 100;
        }
        doWait = function(time) {
          var defer, ret;
          defer = $q.defer();
          if (time === 0) {
            defer.reject('exceed 100 times check');
          }
          ret = func();
          if (check(ret)) {
            defer.resolve(ret);
          } else {
            $timeout((function(_this) {
              return function() {
                return doWait(time - 1).then(function(ret) {
                  return defer.resolve(ret);
                }, function(err) {
                  return defer.reject(err);
                });
              };
            })(this), interval);
          }
          return defer.promise;
        };
        return doWait(maxTime);
      };

      return Util;

    })();
    return new Util();
  }]);

}).call(this);

(function() {
  angular.module('m-directive', ['m-util']);

}).call(this);

(function() {
  
  window.App = angular.module(Config.name, ['templates','ngSanitize', 'ngRoute', 'ngAnimate', 'restangular', 'ui.bootstrap', 'headroom', 'duoshuo', 'config', 'm-directive', 'm-service']);

  angular.module('m-service', ['m-util']);

  angular.module('m-directive', ['m-util']);

  App.constant('Config', Config);

  App.constant('Cache', locache);

  App.constant('_', _);

  App.config(["$provide", "$httpProvider", "RestangularProvider", function($provide, $httpProvider, RestangularProvider) {
    RestangularProvider.setBaseUrl(Config.uri.api);
    RestangularProvider.setDefaultRequestParams('jsonp', {
      callback: 'JSON_CALLBACK'
    });
    return $httpProvider.interceptors.push(["$timeout", "$q", "$rootScope", "$location", function($timeout, $q, $rootScope, $location) {
      return {
        request: function(config) {
          return config || $q.when(config);
        },
        responseError: function(response) {
          var tplErrorHandler;
          if (response.data && response.data.message) {
            tplErrorHandler = 'partials/modal/error_handler.html';
            $rootScope.Util.createDialog(tplErrorHandler, {
              message: response.data.message
            }, function() {});
          }
          return $q.reject(response);
        }
      };
    }]);
  }]);

  App.run(["AppInitService", "$rootScope", "$location", "Config", function(AppInitService, $rootScope, $location, Config) {
    AppInitService.init();
    return $rootScope.$on('$routeChangeSuccess', function($event, current) {
      if (_hmt) {
        return _hmt.push(['_trackPageview', $location.url()]);
      }
    });
  }]);

}).call(this);

(function() {
  App.constant('Constant', {
    dict: {}
  });

}).call(this);


/**
 @ngdoc directive
 @name m-directive.directive:mBreadcrumb
 @element div
 @restrict AE
 @description This directive is used to create breadcrumbs
 @example
    <example module="eg">
    <file name="index.js">
      angular.module('eg',['m-directive'])
        .controller('EgCtrl', function($scope, breadcrumbs){
          $scope.breadcrumbs = breadcrumbs;
        });
    </file>
    <file name="index.html">
      <div ng-controller="EgCtrl">
        <m-breadcrumb></m-breadcrumb>
      </div>
    </file>
    </example>
 */

(function() {
  angular.module('m-directive').directive('mBreadcrumb', function() {
    return {
      restrict: 'AE',
      scope: {},
      controller: ["$scope", "breadcrumbs", function($scope, breadcrumbs) {
        return $scope.breadcrumbs = breadcrumbs;
      }],
      template: "<ol class=\"ab-nav breadcrumb\">\n  <li ng-repeat=\"breadcrumb in breadcrumbs.get() track by breadcrumb.path\"\n    ng-class=\"{ active: $last }\">\n    <a ng-if=\"!$last\" ng-href=\"{{ breadcrumb.path }}\"\n      ng-bind=\"breadcrumb.label\" class=\"margin-right-xs\"></a>\n    <span ng-if=\"$last\" ng-bind=\"breadcrumb.label\"></span>\n  </li>\n</ol>"
    };
  });

}).call(this);

(function() {
  angular.module('m-directive').directive('mCompile', ["$compile", function($compile) {
    return {
      restrict: 'A',
      link: function(scope, element, attrs) {
        return scope.$watch(function() {
          return scope.$eval(attrs.mCompile);
        }, function(value) {
          element.html(value);
          return $compile(element.contents())(scope);
        });
      }
    };
  }]);

}).call(this);

(function() {
  angular.module('m-directive').directive('mDynamicTpl', ["Util", "$rootScope", function(Util, $rootScope) {
    var linker;
    linker = function(scope, element, attrs) {
      scope.Util = Util;
      scope.dict = $rootScope.dict;
      return Util.compileTemplate(scope.tpl, scope).then(function(html) {
        return element.replaceWith(html);
      });
    };
    return {
      restrict: 'E',
      scope: {
        tpl: '=',
        data: '=',
        actions: '='
      },
      link: linker
    };
  }]);

}).call(this);


/**
  @ngdoc directive
  @name m-directive.directive:mFullscreen
  @element div
  @restrict A
  @description This directive is used to toggle fullscreen on an element
  @example
    <example module="eg">
    <file name="index.js">
      angular.module('eg',['m-directive'])
        .controller('EgCtrl', ['$scope', function($scope){
        }]);
    </file>
    <file name="index.html">
      <div ng-controller="EgCtrl">
        <div m-fullscreen style="border: solid 1px">
          <button class="btn btn-primary"
            type="button" data-toggle="m-fullscreen"></button>
        </div>
      </div>
    </file>
    </example>
 */

(function() {
  angular.module('m-directive').directive('mFullscreen', function() {
    var findChild;
    findChild = function(el, func) {
      var child, children, e, find, _i, _len;
      el = angular.element(el);
      children = el.children();
      e = _.find(children, function(d) {
        return func(d);
      });
      if (e) {
        return e;
      } else {
        for (_i = 0, _len = children.length; _i < _len; _i++) {
          child = children[_i];
          find = findChild(child, func);
          if (find) {
            return find;
          }
        }
      }
    };
    return {
      restrict: 'A',
      scope: {},
      link: function(scope, element, attrs) {
        var toggleEl;
        toggleEl = findChild(element, function(d) {
          return 'm-fullscreen' === angular.element(d).attr('data-toggle');
        });
        if (toggleEl) {
          toggleEl = angular.element(toggleEl);
          toggleEl.attr('style', 'cursor:sw-resize');
          toggleEl.bind('click', function() {
            angular.element(document.body).toggleClass('fullscreenStatic');
            return element.toggleClass('fullscreen');
          });
          return scope.$on('$destroy', function() {
            return toggleEl.unbind('click');
          });
        }
      }
    };
  });

}).call(this);

(function() {
  angular.module('m-directive').directive('mLoading', function() {
    return {
      restrict: 'E',
      scope: {},
      template: "<div class=\"loading\">\n<i class=\"fa fa-spinner fa-spin pull-left\">\n</i>\n<h4>{{ text }}</h4>\n</div>",
      link: function(scope, element, attrs) {
        scope.text = attrs.text;
        return scope.text != null ? scope.text : scope.text = 'Loading...';
      }
    };
  });

}).call(this);


/**
 @ngdoc directive
 @name m-directive.directive:mAnnoucement
 @element div
 @restrict AE
 @description This directive is used to create `annoucements`,
 @example
  <example module="eg">
    <file name="index.js">
      angular.module('eg',['ui.bootstrap', 'm-directive'])
        .controller('EgCtrl', ['$scope', function($scope){
          $scope.anns = [{
            date: "2014-01-01",
            msg: "this is a test"
          }];
        }]);
    </file>
    <file name="index.html">
      <div ng-controller="EgCtrl">
        <m-announcement announcements="anns"></m-announcement>
      </div>
    </file>
  </example>
 */

(function() {
  angular.module('m-directive').directive('mAnnouncement', ["$position", function($position) {
    return {
      restrict: 'AE',
      scope: {
        announcements: '='
      },
      template: "<span ng-show=\"show\">\n<i class=\"fa fa-bullhorn animated infinite\"\n   ng-class=\"{flash:flash}\"></i>\n<div class=\"popover right fade in\"\n  style=\"transform:translateY(-40%);word-break:break-word;\">\n  <div class=\"arrow\" style=\"top:90%\"></div>\n  <div class=\"popover-inner\">\n  <h3 class=\"popover-title\" ng-bind=\"title\" ng-show=\"title\"></h3>\n  <div class=\"popover-content\">\n    <alert ng-repeat=\"a in announcements\"\n      type=\"{{a.type || 'success'}}\"\n      style=\"margin:3px;padding:2px\">\n      <p>\n        <div class=\"label label-primary\"\n          style=\"margin-right:5px\">\n          {{a.date | date:'yyyy-MM-dd HH:mm:ss'}}\n        </div>\n        <div style=\"margin-left:5px\">{{a.msg}}</div>\n      </p>\n    </alert>\n  </div>\n  </div>\n</div>\n</span>",
      controller: ["$scope", function($scope) {
        var cacheKey;
        cacheKey = 'viewed_annoucement_date';
        $scope.$watch('announcements', function(newVal, oldVal) {
          var first;
          if (newVal && newVal.length) {
            $scope.show = true;
            first = newVal[0];
            if (new Date(first.date) > new Date(localStorage.getItem(cacheKey))) {
              $scope.flash = true;
            }
            return localStorage.setItem(cacheKey, first.date);
          }
        }, true);
        return $scope.stopFlash = function() {
          return $scope.flash = false;
        };
      }],
      link: function(scope, element, attrs) {
        var icon, list;
        icon = element.find('i');
        list = element.find('div');
        if (list.length) {
          list = angular.element(list[0]);
        }
        scope.title = attrs.title || 'Announcements';
        element.bind('mouseenter', function() {
          var ttPosition;
          scope.$apply(scope.stopFlash);
          list.css('display', 'block');
          ttPosition = $position.positionElements(icon, list, attrs.placement || 'right');
          ttPosition.top += 'px';
          ttPosition.left += 'px';
          return list.css(ttPosition);
        });
        element.bind('mouseleave', function() {
          return list.css('display', 'none');
        });
        return scope.$on('$destroy', function() {
          element.unbind('mouseenter');
          return element.unbind('mouseleave');
        });
      }
    };
  }]);

}).call(this);

(function() {
  angular.module('m-directive').directive('mMoreButton', ["$parse", "$q", function($parse, $q) {
    return {
      restrict: 'E',
      scope: {
        currentCount: '=currentCount',
        loadFunc: '=loadFunc',
        allCount: '=allCount'
      },
      transclude: true,
      templateUrl: 'partials/directive/moreButton.html',
      link: function(scope, element, attrs) {
        scope.has_more = function() {
          return scope.currentCount < scope.allCount;
        };
        scope.loaded = true;
        return scope.show_more = function() {
          scope.loaded = false;
          return scope.loadFunc(scope.currentCount).then(function() {
            return scope.loaded = true;
          });
        };
      }
    };
  }]);

}).call(this);

(function() {
  angular.module('m-directive').directive('mNoResult', function() {
    return {
      restrict: 'E',
      scope: {},
      template: "<div class=\"no-result\">\n  {{ text }}\n</div>",
      link: function(scope, element, attrs) {
        scope.text = attrs.text;
        return scope.text != null ? scope.text : scope.text = 'No Result.';
      }
    };
  });

}).call(this);

(function() {
  angular.module('m-directive').directive('mResize', ["$window", function($window) {
    return {
      restrict: 'A',
      scope: {},
      link: function(scope, element, attrs) {
        var h, isFixed, r, rate, w;
        w = $window;
        h = w.innerHeight;
        rate = attrs.mResize;
        isFixed = attrs.mResizeFixed;
        if (!rate) {
          rate = 100;
        }
        r = rate / 100;
        if (isFixed === 'true') {
          element.css('height', h * r);
          element.css('overflow-y', 'auto');
          return element.css('overflow-x', 'hidden');
        } else {
          return element.css('min-height', h * r);
        }
      }
    };
  }]);

}).call(this);

(function() {
  angular.module('m-directive').directive('mScroll', function() {
    return {
      restrict: 'A',
      link: function(scope, el, attrs) {
        var raw, scrollBottomFunc, scrollFunc;
        scrollFunc = attrs.scroll;
        scrollBottomFunc = attrs.scrollBottom;
        raw = el[0];
        if (scrollFunc || scrollBottomFunc) {
          el.bind('scroll', _.debounce(function() {
            if (scrollFunc) {
              scope.$apply(scrollFunc);
            }
            if (scrollBottomFunc) {
              if (raw.scrollTop + raw.offsetHeight + 5 >= raw.scrollHeight) {
                return scope.$apply(scrollBottomFunc);
              }
            }
          }, 200));
          return scope.$on('destroy', function() {
            return el.unbind('scroll');
          });
        }
      }
    };
  });

}).call(this);

(function() {
  App.factory("animateCSSBuild", [
    "$timeout", function($timeout) {
      var defaultDelay;
      defaultDelay = 500;
      return function(baseClass, classNames) {
        var a, animateCSSEnd, animateCSSStart, b, timeoutKey;
        if (arguments.length === 3) {
          a = classNames;
          b = arguments[2];
          classNames = {
            enter: a,
            move: a,
            leave: b,
            show: a,
            hide: b,
            addClass: a,
            removeClass: b
          };
        }
        timeoutKey = "$$animate.css-timer";
        animateCSSStart = function(element, className, delay, done) {
          var timer;
          element.addClass(className);
          element.addClass("animated");
          timer = $timeout(done, delay || defaultDelay, false);
          element.data(timeoutKey, timer);
        };
        animateCSSEnd = function(element, className) {
          return function(cancelled) {
            var timer;
            timer = element.data(timeoutKey);
            if (timer) {
              $timeout.cancel(timer);
              element.removeData(timeoutKey);
            }
            element.removeClass(className);
            element.removeClass("animated");
          };
        };
        return {
          enter: function(element, done) {
            animateCSSStart(element, classNames.enter, classNames.delay, done);
            return animateCSSEnd(element, classNames.enter);
          },
          leave: function(element, done) {
            animateCSSStart(element, classNames.leave, classNames.delay, done);
            return animateCSSEnd(element, classNames.leave);
          },
          move: function(element, done) {
            animateCSSStart(element, classNames.move, classNames.delay, done);
            return animateCSSEnd(element, classNames.move);
          },
          beforeAddClass: function(element, className, done) {
            var klass;
            klass = className === "ng-hide" && (classNames.hide || (angular.isFunction(classNames.addClass) ? classNames.addClass(className) : classNames.addClass));
            if (klass) {
              animateCSSStart(element, klass, classNames.delay, done);
              return animateCSSEnd(element, klass);
            }
            done();
          },
          addClass: function(element, className, done) {
            var klass;
            klass = className !== "ng-hide" && (angular.isFunction(classNames.addClass) ? classNames.addClass(className) : classNames.addClass);
            if (klass) {
              animateCSSStart(element, klass, classNames.delay, done);
              return animateCSSEnd(element, klass);
            }
            done();
          },
          removeClass: function(element, className, done) {
            var klass;
            klass = (className === "ng-hide" && classNames.show) || (angular.isFunction(classNames.removeClass) ? classNames.removeClass(className) : classNames.removeClass);
            if (klass) {
              animateCSSStart(element, klass, classNames.delay, done);
              return animateCSSEnd(element, klass);
            }
            done();
          }
        };
      };
    }
  ]).animation(".animate-flip-x", [
    "animateCSSBuild", function(animateCSSBuild) {
      return animateCSSBuild("animate-flip-x", "flipInX", "flipOutX");
    }
  ]).animation(".animate-flip-y", [
    "animateCSSBuild", function(animateCSSBuild) {
      return animateCSSBuild("animate-flip-y", "flipInY", "flipOutY");
    }
  ]).animation(".animate-fade", [
    "animateCSSBuild", function(animateCSSBuild) {
      return animateCSSBuild("animate-fade", "fadeIn", "fadeOut");
    }
  ]).animation(".animate-fade-up", [
    "animateCSSBuild", function(animateCSSBuild) {
      return animateCSSBuild("animate-fade-up", "fadeInUp", "fadeOutUp");
    }
  ]).animation(".animate-fade-down", [
    "animateCSSBuild", function(animateCSSBuild) {
      return animateCSSBuild("animate-fade-down", "fadeInDown", "fadeOutDown");
    }
  ]).animation(".animate-fade-left", [
    "animateCSSBuild", function(animateCSSBuild) {
      return animateCSSBuild("animate-fade-left", "fadeInLeft", "fadeOutLeft");
    }
  ]).animation(".animate-fade-right", [
    "animateCSSBuild", function(animateCSSBuild) {
      return animateCSSBuild("animate-fade-right", "fadeInRight", "fadeOutRight");
    }
  ]).animation(".animate-fade-up-big", [
    "animateCSSBuild", function(animateCSSBuild) {
      return animateCSSBuild("animate-fade-up-big", "fadeInUpBig", "fadeOutUpBig");
    }
  ]).animation(".animate-fade-down-big", [
    "animateCSSBuild", function(animateCSSBuild) {
      return animateCSSBuild("animate-fade-down-big", "fadeInDownBig", "fadeOutDownBig");
    }
  ]).animation(".animate-fade-left-big", [
    "animateCSSBuild", function(animateCSSBuild) {
      return animateCSSBuild("animate-fade-left-big", "fadeInLeftBig", "fadeOutLeftBig");
    }
  ]).animation(".animate-fade-right-big", [
    "animateCSSBuild", function(animateCSSBuild) {
      return animateCSSBuild("animate-fade-right-big", "fadeInRightBig", "fadeOutRightBig");
    }
  ]).animation(".animate-bounce", [
    "animateCSSBuild", function(animateCSSBuild) {
      return animateCSSBuild("animate-bounce", "bounceIn", "bounceOut");
    }
  ]).animation(".animate-bounce-up", [
    "animateCSSBuild", function(animateCSSBuild) {
      return animateCSSBuild("animate-bounce-up", "bounceInUp", "bounceOutUp");
    }
  ]).animation(".animate-bounce-down", [
    "animateCSSBuild", function(animateCSSBuild) {
      return animateCSSBuild("animate-bounce-down", "bounceInDown", "bounceOutDown");
    }
  ]).animation(".animate-bounce-left", [
    "animateCSSBuild", function(animateCSSBuild) {
      return animateCSSBuild("animate-bounce-left", "bounceInLeft", "bounceOutLeft");
    }
  ]).animation(".animate-bounce-right", [
    "animateCSSBuild", function(animateCSSBuild) {
      return animateCSSBuild("animate-bounce-right", "bounceInRight", "bounceOutRight");
    }
  ]).animation(".animate-rotate", [
    "animateCSSBuild", function(animateCSSBuild) {
      return animateCSSBuild("animate-rotate", "rotateIn", "rotateOut");
    }
  ]).animation(".animate-rotate-up-left", [
    "animateCSSBuild", function(animateCSSBuild) {
      return animateCSSBuild("animate-rotate-up-left", "rotateInUpLeft", "rotateOutUpLeft");
    }
  ]).animation(".animate-rotate-down-left", [
    "animateCSSBuild", function(animateCSSBuild) {
      return animateCSSBuild("animate-rotate-down-left", "rotateInDownLeft", "rotateOutDownLeft");
    }
  ]).animation(".animate-rotate-up-right", [
    "animateCSSBuild", function(animateCSSBuild) {
      return animateCSSBuild("animate-rotate-up-right", "rotateInUpRight", "rotateOutUpRight");
    }
  ]).animation(".animate-rotate-down-right", [
    "animateCSSBuild", function(animateCSSBuild) {
      return animateCSSBuild("animate-rotate-down-right", "rotateInDownRight", "rotateOutDownRight");
    }
  ]).animation(".animate-lightspeed", [
    "animateCSSBuild", function(animateCSSBuild) {
      return animateCSSBuild("animate-lightspeed", "lightSpeedIn", "lightSpeedOut");
    }
  ]).animation(".animate-roll", [
    "animateCSSBuild", function(animateCSSBuild) {
      return animateCSSBuild("animate-roll", "rollIn", "rollOut");
    }
  ]).animation(".animate-hinge", [
    "animateCSSBuild", function(animateCSSBuild) {
      return animateCSSBuild("animate-hinge", "fadeIn", "hinge");
    }
  ]);

}).call(this);

(function() {
  App.factory('AppInitService', ["$rootScope", "$window", "IntroService", "$q", "Util", "Config", "Cache", "Constant", function($rootScope, $window, IntroService, $q, Util, Config, Cache, Constant) {
    var promises;
    promises = [];
    return {
      init: function() {
        var persistenceDefer;
        $rootScope.$on('$routeChangeSuccess', function($event, current) {
          return $rootScope.currentPage = current.name;
        });
        $rootScope.Util = Util;
        $rootScope.config = Config;
        $rootScope.user = angular.fromJson(localStorage.getItem('user'));
        $rootScope.dict = {
          get: function(key) {
            var ret;
            ret = Constant.dict[key];
            if (ret == null) {
              ret = key;
            }
            return ret;
          }
        };
        $rootScope.startIntro = function() {
          return IntroService.start();
        };
        $rootScope.persistence = {};
        persistenceDefer = $q.defer();
        promises.push(persistenceDefer);
        return $rootScope.$watch((function(_this) {
          return function() {
            return angular.toJson($rootScope.persistence);
          };
        })(this), (function(_this) {
          return function(newVal, oldVal) {
            var key;
            key = 'persistent_object';
            if (newVal === oldVal) {
              _.extend($rootScope.persistence, Cache.get(key));
              return persistenceDefer.resolve();
            } else {
              return Cache.set(key, JSON.parse(newVal));
            }
          };
        })(this), true);
      },
      add: function(promise) {
        return promises.push(promise);
      },
      done: function() {
        return $q.all(promises);
      }
    };
  }]);

}).call(this);

(function() {
  
  App.factory('BlogService', ["BlogRemoteService", function(BlogRemoteService) {
    return {
      decorateBlog: function(blog) {
        var e, meta, metaStr;
        if (!blog.body) {
          return blog;
        }
        metaStr = blog.body.substring(0, blog.body.indexOf('-->'));
        metaStr = metaStr.replace(/\n|\r|<!-{2,}/gm, ' ');
        try {
          meta = JSON.parse(metaStr);
        } catch (_error) {
          e = _error;
          console.log(e);
        }
        blog.meta = meta;
        if (blog.meta && blog.meta.summary) {
          BlogRemoteService.renderMarkdown(blog.meta.summary).then(function(data) {
            return blog.meta.summary = data;
          });
        }
        return blog;
      }
    };
  }]);

}).call(this);

(function() {
  App.factory("breadcrumbs", [
    "$rootScope", "$location", "$route", function($rootScope, $location, $route) {
      var BreadcrumbService;
      BreadcrumbService = {
        breadcrumbs: [],
        get: function() {
          var key, self;
          if (this.options) {
            self = this;
            for (key in this.options) {
              if (this.options.hasOwnProperty(key)) {
                angular.forEach(self.breadcrumbs, function(breadcrumb) {
                  if (breadcrumb.label === key) {
                    breadcrumb.label = self.options[key];
                  }
                });
              }
            }
          }
          return this.breadcrumbs;
        },
        generateBreadcrumbs: function() {
          var getRoute, path, pathElements, routes, self;
          routes = $route.routes;
          pathElements = $location.path().split("/");
          path = "";
          self = this;
          getRoute = function(route) {
            var param;
            param = void 0;
            angular.forEach($route.current.params, function(value, key) {
              var re;
              re = new RegExp(value);
              if (re.test(route)) {
                param = value;
              }
              route = route.replace(re, ":" + key);
            });
            return {
              path: route,
              param: param
            };
          };
          if (pathElements[1] === "") {
            delete pathElements[1];
          }
          this.breadcrumbs = [];
          angular.forEach(pathElements, function(el) {
            var label, route;
            path += (path === "/" ? el : "/" + el);
            route = getRoute(path);
            if (routes[route.path]) {
              label = routes[route.path].label || route.param;
              self.breadcrumbs.push({
                label: label,
                path: path
              });
            }
          });
        }
      };
      $rootScope.$on("$routeChangeSuccess", function() {
        BreadcrumbService.generateBreadcrumbs();
      });
      return BreadcrumbService;
    }
  ]);

}).call(this);

(function() {
  
  App.factory('IntroService', ["Config", "Cache", "$timeout", function(Config, Cache, $timeout) {
    var cacheKey, initIntro, intro;
    intro = null;
    cacheKey = "" + Config.name + "_IntroService_init";
    initIntro = function() {
      var els, steps;
      if (typeof introJs !== "undefined" && introJs !== null) {
        steps = Config.intros;
        els = document.querySelectorAll('.intro-step');
        steps = _.map(steps, function(step, i) {
          var findEl;
          findEl = _.find(els, function(e) {
            var num;
            e = angular.element(e);
            num = e.attr('intro-step');
            return i === parseInt(num - 1);
          });
          if (findEl) {
            step.element = findEl;
          }
          return step;
        });
        intro = introJs();
        return intro.setOptions({
          steps: steps
        });
      }
    };
    return {
      init: function() {
        var isInit, start;
        initIntro();
        isInit = Cache.get(cacheKey);
        if (!isInit) {
          start = this.start;
          return $timeout(function() {
            if (start()) {
              return Cache.set(cacheKey, true);
            }
          }, 1500);
        }
      },
      start: function() {
        if (intro) {
          intro.start();
          return true;
        } else {
          return false;
        }
      }
    };
  }]);

}).call(this);

(function() {
  
  App.factory('LoadingService', ["$rootScope", "$q", "$timeout", "Util", function($rootScope, $q, $timeout, Util) {
    return {
      init: function() {
        var loadingDefer;
        loadingDefer = $q.defer();
        $rootScope.$watch((function() {
          return $rootScope.loading;
        }), function() {
          if ($rootScope.loading != null) {
            if ($rootScope.loading === 'loaded') {
              if ($rootScope.loadDialog != null) {
                $rootScope.initializing = 100;
                $timeout(function() {
                  return $rootScope.loadDialog.close();
                }, 1000);
              }
              $rootScope.initiated = true;
              return loadingDefer.resolve();
            }
          } else {
            $rootScope.initializing = 10;
            return $rootScope.loadDialog = Util.createDialog('partials/modal/loading.html', null, angular.noop, {
              backdrop: 'static'
            });
          }
        });
        return loadingDefer.promise;
      },
      done: function() {
        return $rootScope.loading = 'loaded';
      }
    };
  }]);

}).call(this);

(function() {
  
  App.factory('NProgressService', ["$timeout", function($timeout) {
    return {
      start: function() {
        if (NProgress) {
          return NProgress.start();
        }
      },
      done: function() {
        if (NProgress) {
          return $timeout(function() {
            return NProgress.done();
          }, 1000);
        }
      }
    };
  }]);

}).call(this);

(function() {
  
  App.factory('PiwikService', ["Config", function(Config) {
    return {
      init: function(username, pagename) {
        var app, piwikTracker, pkBaseURL, prod, siteId;
        if (typeof Piwik !== "undefined" && Piwik !== null) {
          siteId = Config.piwik.siteId;
          pkBaseURL = Config.piwik.url;
          app = Config.piwik.app;
          prod = Config.piwik.prod;
          piwikTracker = Piwik.getTracker("" + pkBaseURL + "/piwik.php", siteId);
          piwikTracker.setCustomVariable(1, "User", username, "visit");
          piwikTracker.setCustomVariable(2, "App", app, "page");
          piwikTracker.setCustomVariable(3, "PageName", pagename, "page");
          piwikTracker.setCustomVariable(4, "Prod", prod, "page");
          piwikTracker.trackPageView();
          return piwikTracker.enableLinkTracking();
        }
      }
    };
  }]);

}).call(this);

(function() {
  
  App.factory('BlogRemoteService', ["Config", "Restangular", "Util", function(Config, Restangular, Util) {
    var defaultParam, getCacheKey, getWithCache, repo, rest;
    repo = Config.githubRepo;
    rest = Restangular.all("repos/" + repo.owner + "/" + repo.name);
    getCacheKey = function(method, param) {
      return "" + Config.name + "_BlogRemoteservice_" + method + "_" + (JSON.stringify(param));
    };
    getWithCache = function(method, param, func, timeout) {
      return Util.getWithCache(getCacheKey(method, param), true, func, 300);
    };
    defaultParam = {
      state: "open",
      labels: !Config.debug ? "blog" : '',
      page: 1,
      per_page: 10
    };
    this.getBlogs = function(param) {
      param = _.extend(defaultParam, param);
      return getWithCache('issues', param, function() {
        return rest.one('issues').get(param);
      });
    };
    this.getBlog = function(id, param) {
      param = _.extend(defaultParam, param);
      return getWithCache('issues' + id, param, function() {
        return rest.one('issues/' + id).get(param);
      });
    };
    this.renderMarkdown = function(text) {
      return getWithCache('markdown', text, function() {
        return Restangular.all('markdown').post({
          text: text,
          mode: 'gfm'
        });
      });
    };
    return this;
  }]);

}).call(this);

(function() {
  
  App.factory('SampleRemoteService', ["Config", "Restangular", "Util", function(Config, Restangular, Util) {
    var getCacheKey, getWithCache, rest;
    rest = Restangular.all('ops');
    getCacheKey = function(method, param) {
      return "" + Config.name + "_SampleRemoteservice_" + method + "_" + (JSON.stringify(param));
    };
    getWithCache = function(method, param, func, timeout) {
      return Util.getWithCache(getCacheKey(method, param), true, func, timeout);
    };
    this.query = function(param, canceler) {
      return getWithCache('query', param, function() {
        if (canceler && canceler.promise) {
          return rest.one('query').withHttpConfig({
            timeout: canceler.promise
          }).get(param);
        } else {
          return rest.one('query').get(param);
        }
      });
    };
    return this;
  }]);

}).call(this);

(function() {
  var __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; },
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  App.factory('ArticleViewModel', ["$rootScope", "$routeParams", "BlogRemoteService", "BlogService", "BaseViewModel", "Util", "Cache", function($rootScope, $routeParams, BlogRemoteService, BlogService, BaseViewModel, Util, Cache) {
    var ArticleViewModel;
    return ArticleViewModel = (function(_super) {
      __extends(ArticleViewModel, _super);

      function ArticleViewModel() {
        this.bindAction = __bind(this.bindAction, this);
        this.bindView = __bind(this.bindView, this);
        return ArticleViewModel.__super__.constructor.apply(this, arguments);
      }

      ArticleViewModel.prototype.bindView = function() {
        var id;
        id = $routeParams.id;
        return BlogRemoteService.getBlog(id).then((function(_this) {
          return function(blog) {
            if (blog.body) {
              _this.data.blog = BlogService.decorateBlog(blog);
              BlogRemoteService.renderMarkdown(blog.body).then(function(ret) {
                return _this.data.content = ret;
              });
              return $rootScope.blog = _this.data.blog;
            }
          };
        })(this));
      };

      ArticleViewModel.prototype.bindAction = function() {};

      return ArticleViewModel;

    })(BaseViewModel);
  }]);

}).call(this);

(function() {
  var __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

  App.factory('BaseViewModel', ["$q", "$location", "PiwikService", "$timeout", "$rootScope", "AppInitService", "Config", "LoadingService", "IntroService", "NProgressService", function($q, $location, PiwikService, $timeout, $rootScope, AppInitService, Config, LoadingService, IntroService, NProgressService) {
    var BaseViewModel;
    return BaseViewModel = (function() {
      function BaseViewModel(scope) {
        this.scope = scope;
        this.pageInit = __bind(this.pageInit, this);
        this.bindAction = __bind(this.bindAction, this);
        this.bindView = __bind(this.bindView, this);
        this.initialize = __bind(this.initialize, this);
        this.state = {};
        this.data = {};
        this.actions = this.bindAction();
        AppInitService.done().then((function(_this) {
          return function() {
            _this.pageInit().then(_this.bindView);
            if (Config.debug === true) {
              window.vm = _this;
              return window.scope = _this.scope;
            }
          };
        })(this));
      }

      BaseViewModel.prototype.initialize = function() {
        var defer;
        defer = $q.defer();
        defer.resolve();
        return defer.promise;
      };

      BaseViewModel.prototype.bindView = function() {};

      BaseViewModel.prototype.bindAction = function() {};

      BaseViewModel.prototype.logout = function() {
        localStorage.clear();
        return $location.url('/logout');
      };

      BaseViewModel.prototype.pageInit = function() {
        var defer;
        defer = $q.defer();
        NProgressService.start();
        this.initialize().then((function(_this) {
          return function() {
            NProgressService.done();
            if (Config.intro.enabled) {
              IntroService.init();
            }
            if (Config.piwik.enabled && _this.scope.user) {
              PiwikService.init(_this.scope.user.nt, $rootScope.currentPage);
            }
            return defer.resolve();
          };
        })(this));
        return defer.promise;
      };

      return BaseViewModel;

    })();
  }]);

}).call(this);

(function() {
  var __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; },
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  App.factory('HomeViewModel', ["$q", "$location", "$timeout", "BlogRemoteService", "BlogService", "BaseViewModel", "Util", "Cache", function($q, $location, $timeout, BlogRemoteService, BlogService, BaseViewModel, Util, Cache) {
    var HomeViewModel;
    return HomeViewModel = (function(_super) {
      __extends(HomeViewModel, _super);

      function HomeViewModel() {
        this.bindAction = __bind(this.bindAction, this);
        this.bindView = __bind(this.bindView, this);
        return HomeViewModel.__super__.constructor.apply(this, arguments);
      }

      HomeViewModel.prototype.bindView = function() {
        return BlogRemoteService.getBlogs().then((function(_this) {
          return function(blogs) {
            return _this.data.blogs = _this.processBlogs(blogs);
          };
        })(this));
      };

      HomeViewModel.prototype.processBlogs = function(blogs) {
        return _.map(blogs, BlogService.decorateBlog);
      };

      HomeViewModel.prototype.bindAction = function() {};

      return HomeViewModel;

    })(BaseViewModel);
  }]);

}).call(this);

angular.module('templates', ['partials/404.html', 'partials/about.html', 'partials/article.html', 'partials/directive/moreButton.html', 'partials/home.html', 'partials/logout.html', 'partials/modal/alert.html', 'partials/modal/error_handler.html', 'partials/modal/loading.html']);

angular.module("partials/404.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("partials/404.html",
    "<div class=\"container\">\n" +
    "  <h1>404! Page Not Found!</h1>\n" +
    "</div>\n" +
    "");
}]);

angular.module("partials/about.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("partials/about.html",
    "<p>Martin Liu</p>\n" +
    "<p>Web Architect</p>\n" +
    "<p>Github: <a href=\"http://github.com/martin-liu\" target=\"_blank\">http://github.com/martin-liu</a></p>\n" +
    "<p>Email: <a href=\"mailto:hflh1989@gmail.com\">hflh1989@gmail.com</a></p>\n" +
    "");
}]);

angular.module("partials/article.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("partials/article.html",
    "<m-loading ng-if=\"!vm.data.content\"></m-loading>\n" +
    "<div ng-if=\"vm.data.content\">\n" +
    "  <h2 class=\"align-center\" ng-bind=\"vm.data.blog.title\"></h2>\n" +
    "  <p ng-bind=\"vm.data.blog.created_at | date:'yyyy-MM-dd hh:mm:ss'\" class=\"created-at\"></p>\n" +
    "  <br/>\n" +
    "  <div class=\"blog-content markdown-body\" ng-bind-html=\"vm.data.content\"></div>\n" +
    "</div>\n" +
    "\n" +
    "<br/>\n" +
    "<br/>\n" +
    "<hr/>\n" +
    "<p>欢迎扫码订阅公众号:</p>\n" +
    "<img width=\"120\" src=\"/image/qrcode_wechat.jpg\"/>\n" +
    "<div ng-if=\"vm.data.blog.number\"\n" +
    "     duoshuo data-thread-key=\"{{vm.data.blog.number}}\"></div>\n" +
    "");
}]);

angular.module("partials/directive/moreButton.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("partials/directive/moreButton.html",
    "<div style=\"margin: 5px\" ng-if=\"allNum > 0\">\n" +
    "    <div>\n" +
    "        <div class=\"pull-right\">\n" +
    "            <span style=\"vertical-align:middle;color: grey;font-size: 15px\"><strong>{{data.length}} of {{ allNum }}</strong></span>\n" +
    "            <button ng-if=\"has_more()\" class=\"btn action-btn details-btn\" style=\"float: none\" ng-click=\"show_more()\" ng-disabled=\"!loaded\">\n" +
    "                <i class=\"icon-plus icon-white\"></i>More\n" +
    "            </button>\n" +
    "        </div>\n" +
    "        <div class=\"clearfix\" style=\"margin: 5px\"></div>\n" +
    "        <div class=\"pull-right\">\n" +
    "            {{ loaded ? '' : 'Loading...' }}\n" +
    "        </div>\n" +
    "    </div>\n" +
    "</div>");
}]);

angular.module("partials/home.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("partials/home.html",
    "<m-loading ng-if=\"!vm.data.blogs\"></m-loading>\n" +
    "<div ng-if=\"vm.data.blogs\" ng-repeat=\"blog in vm.data.blogs\">\n" +
    "  <a class=\"blog-title-link\" ng-href=\"#!/article/{{blog.number}}\">\n" +
    "    <h3 ng-bind=\"blog.title\"></h3>\n" +
    "  </a>\n" +
    "  <p class=\"summary markdown-body\" ng-bind-html=\"blog.meta.summary\"></p>\n" +
    "  <p>\n" +
    "    <a ng-href=\"#!/article/{{blog.number}}\">阅读全文...</a>\n" +
    "  </p>\n" +
    "  <span ng-bind=\"blog.created_at | date:'yyyy-MM-dd'\"></span>\n" +
    "  <hr/>\n" +
    "</div>\n" +
    "");
}]);

angular.module("partials/logout.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("partials/logout.html",
    "<div class=\"row-fluid alert\">\n" +
    "    <h4>You are not logged in.</h4>\n" +
    "    <h4><a href=\"\" onclick=\"javascript:location.reload()\">Log In Now</a></h4>\n" +
    "</div>");
}]);

angular.module("partials/modal/alert.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("partials/modal/alert.html",
    "<div>\n" +
    "    <div class=\"modal-header\" style=\"padding: 0 15px\">\n" +
    "        <h4 ng-if=\"type == 'success'\">Success!</h4>\n" +
    "        <h4 ng-if=\"type == 'fail'\">Fail!</h4>\n" +
    "        <h4 ng-if=\"type == 'confirm'\">Confirm</h4>\n" +
    "    </div>\n" +
    "    <div class=\"modal-body\">\n" +
    "        <p class=\"alert {{class}}\" ng-bind-html=\"message\"></p>\n" +
    "    </div>\n" +
    "    <div class=\"modal-footer\" style=\"padding: 5px 20px\">\n" +
    "      <div ng-if=\"type == 'confirm'\">\n" +
    "        <button ng-click=\"close(true)\" class=\"btn btn-primary\" >Yes</button>\n" +
    "        <button ng-click=\"close(false)\" class=\"btn btn-primary\" >No</button>\n" +
    "      </div>\n" +
    "      <div ng-if=\"type != 'confirm'\">\n" +
    "        <button ng-click=\"close()\" class=\"btn btn-primary\" >Close</button>\n" +
    "      </div>\n" +
    "    </div>\n" +
    "</div>\n" +
    "");
}]);

angular.module("partials/modal/error_handler.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("partials/modal/error_handler.html",
    "<div class=\"{{ animate }}\">\n" +
    "    <div class=\"modal-header\">\n" +
    "        <h4>Request Error</h4>\n" +
    "    </div>\n" +
    "    <div class=\"modal-body\">\n" +
    "        <p class=\"alert alert-danger\">\n" +
    "            {{ message ? message : dict.get('unkownError') }}\n" +
    "        </p>\n" +
    "    </div>\n" +
    "    <div class=\"modal-footer\">\n" +
    "        <button ng-click=\"close()\" class=\"btn btn-primary\" >Close</button>\n" +
    "    </div>\n" +
    "</div>\n" +
    "");
}]);

angular.module("partials/modal/loading.html", []).run(["$templateCache", function($templateCache) {
  $templateCache.put("partials/modal/loading.html",
    "<div class=\"modal-content\">\n" +
    "    <div class=\"modal-header\">\n" +
    "        <h3>Initializing...</h3>\n" +
    "    </div>\n" +
    "    <div class=\"modal-body\">\n" +
    "        <div class=\"progress progress-striped active\">\n" +
    "            <div class=\"progress-bar\" style=\"width: {{ initializing }}%\" role=\"progressbar\" aria-valuenow=\"{{ initializing }}\" aria-valuemin=\"0\" aria-valuemax=\"100\"></div>\n" +
    "        </div>\n" +
    "    </div>\n" +
    "</div>\n" +
    "");
}]);
